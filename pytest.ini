[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    slow: marks tests as slow
    integration: marks integration tests
    snowflake: marks tests requiring Snowflake connection
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning:pandas.*
